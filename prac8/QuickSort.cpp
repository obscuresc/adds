// QuickSort.cpp
#include "QuickSort.h"

std::vector<int> sort(std::vector<int> list) {

	// spec requires pivot be chosen as the second value in the list
	// so long as long as list has more than two elements
	// recursion would be easy but try not to
	// need pivot value, pivot final location, sublish low element location
	// sublist high element location, 
	// while not sorted, use high and low element to map out pivot
	// 'shuffle' sub list, move sublist high and low with respect to
	// final pivot location. If final pivot location is left of previous pivot
	// maybe list of previous pivot values/locations
	return list;	

}
